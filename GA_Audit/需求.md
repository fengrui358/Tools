# 需求

编写 python 脚本，转换Word下的文件，将其全部转换为pdf文件，放在根目录下 pdf_年月日时分秒 命名的文件夹下，如果 word 的名称中不是 2024年 或 2025 年开头的字符串，则将文件名前面的内容删除，只保留后面的文件名。

使用 uv 进行包依赖管理；

调用大模型进行内容检查，判断每个文件中的内容是否对应考核要求且不扣分，提示我哪些地方需要修改；

判断文档中出现的人名是不是都在“人员名单”文件夹下，如果不在需要提示我；

大模型使用 gml，我已经下载到了本地，在 /Users/free/WorkSpace/GLM-API 这个目录下，使用 gml 进行调用；
